# react-day-picker@8.x peer-depends on date-fns v2/v3, but this project uses
# date-fns v4. npm resolves it fine with peer overrides; this makes the install
# non-interactive and consistent on CI/Vercel (avoids ERESOLVE failures).
legacy-peer-deps=true
